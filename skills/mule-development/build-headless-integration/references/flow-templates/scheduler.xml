<?xml version="1.0" encoding="UTF-8"?>
<!--
  Scheduler-triggered flow template. Replace __TOKENS__ and the flow body.
  See: references/flow-templates/README.md
-->
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:__PREFIX__="__NAMESPACE__"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        __NAMESPACE__ __SCHEMA_LOCATION__
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

    <configuration-properties file="config.yaml"
        doc:name="Configuration Properties"
        doc:description="Loads externalized configuration from config.yaml" />

    <__PREFIX__:__CONFIG_ELEMENT__ name="__CONFIG_NAME__"
        doc:name="__PREFIX__ Config"
        doc:description="__PREFIX__ connection (__PROVIDER_NAME__ auth)">
        <__PREFIX__:__PROVIDER_ELEMENT__
            __REQUIRED_FIELD__="${__CONFIG_KEY_FOR_FIELD__}" />
    </__PREFIX__:__CONFIG_ELEMENT__>

    <flow name="__FLOW_NAME__"
        doc:name="__FLOW_DISPLAY_NAME__"
        doc:description="__FLOW_DESCRIPTION__">

        <scheduler doc:name="Every N seconds"
            doc:description="Polls __SOMETHING__ on a fixed schedule">
            <scheduling-strategy>
                <fixed-frequency frequency="60000" timeUnit="MILLISECONDS"/>
            </scheduling-strategy>
        </scheduler>

        <__PREFIX__:__OP_ELEMENT__ config-ref="__CONFIG_NAME__"
            doc:name="__OP_DISPLAY_NAME__"
            doc:description="__OP_DESCRIPTION__"
            target="__RESULT_VAR__">
            <!-- Inline DSL children if the op needs them. Most ops only use attributes. -->
        </__PREFIX__:__OP_ELEMENT__>

        <logger level="INFO"
            message="#[output text/plain --- 'Processed ' ++ sizeOf(vars.__RESULT_VAR__) ++ ' records']"
            doc:name="Log Result Count"
            doc:description="Reports how many records the operation returned" />

        <error-handler>
            <on-error-propagate type="ANY"
                doc:name="On Error"
                doc:description="Logs the error and propagates upstream">
                <logger level="ERROR"
                    message="#[output text/plain --- 'Flow error: ' ++ (error.description default 'no description')]"
                    doc:name="Log Error Detail"
                    doc:description="Logs the error for debugging" />
            </on-error-propagate>
        </error-handler>

    </flow>
</mule>
