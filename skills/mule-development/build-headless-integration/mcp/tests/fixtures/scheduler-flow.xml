<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:salesforce="http://www.mulesoft.org/schema/mule/salesforce"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/salesforce http://www.mulesoft.org/schema/mule/salesforce/current/mule-salesforce.xsd">

    <salesforce:sfdc-config name="sf">
        <salesforce:basic username="x" password="y"/>
    </salesforce:sfdc-config>

    <flow name="poller-flow" doc:name="Poll Salesforce Every 60s">
        <scheduler doc:name="Every 60s">
            <scheduling-strategy>
                <fixed-frequency frequency="60000" timeUnit="MILLISECONDS"/>
            </scheduling-strategy>
        </scheduler>
        <salesforce:query config-ref="sf"
            soql="SELECT Id FROM Account LIMIT 10"
            target="accounts"
            doc:name="Query Recent Accounts"/>
        <logger level="INFO" message="#[sizeOf(vars.accounts)]" doc:name="Log Count"/>
    </flow>
</mule>
